#include <iostream>
using namespace std;

int main() {
    // this fast up the input and output so you might
    // avoid TLE in problems which have lage input or output
    std::ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);
    return 0;
}
